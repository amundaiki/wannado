---
phase: 07-seilturer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/seilturer/page.tsx
  - src/components/ui/ContactForm.tsx
autonomous: true
requirements:
  - PAGE-02
  - CONT-04

must_haves:
  truths:
    - "Hero viser ekte Havsul-bilde (allerede OK, men verifiser at width/height er satt)"
    - "Siden har en bildeseksjon med 4-6 bilder fra scraped/sailing/"
    - "Priser viser kr 350,- per person og kr 3 500,- charter per time"
    - "Praktisk info viser Pollen i Arendal, 2-3 timer, maks 12 personer"
    - "JSON-LD TouristTrip schema finnes i sidekilden"
    - "CTA-knapper lenker til /kontakt?tjeneste=Seilturer og skjemaet forhåndsvelger Seilturer"
  artifacts:
    - path: "src/app/seilturer/page.tsx"
      provides: "Komplett seilturside med galleri, JSON-LD og CTA"
      contains: "application/ld+json"
    - path: "src/components/ui/ContactForm.tsx"
      provides: "Kontaktskjema med defaultService prop"
      exports: ["ContactForm"]
  key_links:
    - from: "src/app/seilturer/page.tsx"
      to: "/kontakt?tjeneste=Seilturer"
      via: "Link href with query param"
      pattern: "kontakt.*tjeneste.*Seilturer"
    - from: "src/components/ui/ContactForm.tsx"
      to: "URL searchParams"
      via: "useSearchParams or defaultService prop"
      pattern: "defaultService|searchParams"
---

<objective>
Polish the seilturer (sailing trips) page with a real image gallery from scraped sailing photos, add JSON-LD TouristTrip structured data for SEO, and wire CTAs to pre-select "Seilturer" on the contact form.

Purpose: This is one of Wannado's primary revenue services. A polished page with real imagery, correct pricing, and structured data will improve both conversion and search ranking for "seiltur Arendal" keywords.

Output: Complete, production-ready /seilturer page with gallery, schema markup, and smart CTA routing.
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/scraped/seilturer.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/ContactForm.tsx (current, no props):
```typescript
"use client";
const serviceOptions = [
  "Seilturer",
  "Kajakktur",
  "Kajakkurs",
  "Teambuilding",
  "Foredrag/JTI",
  "Annet",
];
export default function ContactForm() { ... }
// select element uses defaultValue="" with name="service"
```

From src/app/kontakt/page.tsx:
```typescript
// Uses <ContactForm /> with no props
// Page is a server component (no "use client")
```

From src/app/page.tsx (JSON-LD pattern):
```typescript
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'TouristAttraction',
      // ...
    }),
  }}
/>
```

Available sailing images (all exist, verified):
- public/images/havsul.jpg (753KB, hero - already used)
- public/images/baat-fb.jpg (633KB, Havsul Facebook promo)
- public/images/scraped/sailing/baat-fb-havsul.jpg (633KB)
- public/images/scraped/sailing/P7300114.jpg (358KB)
- public/images/scraped/sailing/P9010367.jpg (411KB)
- public/images/scraped/sailing/hav-1-2.jpg (260KB)
- public/images/scraped/sailing/hav-1-2-1.jpg (260KB, likely duplicate of hav-1-2)
- public/images/scraped/sailing/hav-1-2-2.jpg (260KB, likely duplicate of hav-1-2)
- public/images/scraped/sailing/hav-2-2.jpg (344KB)
- public/images/scraped/sailing/hav-4-2.jpg (348KB)
- public/images/scraped/sailing/hav-7-2.jpg (332KB)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add image gallery, JSON-LD TouristTrip schema, and CTA query params to seilturer page</name>
  <files>src/app/seilturer/page.tsx</files>
  <action>
Modify the existing seilturer page with these three additions:

**1. Image gallery section** - Add between the main content section and the "Hva kan du forvente?" section:
- Create a new section with heading "Bilder fra Havsul" (or similar)
- Display 6 unique images in a responsive grid: 3 columns on desktop, 2 on tablet, 1 on mobile
- Use these images (skip likely duplicates hav-1-2-1 and hav-1-2-2):
  - `/images/scraped/sailing/P7300114.jpg`
  - `/images/scraped/sailing/P9010367.jpg`
  - `/images/scraped/sailing/hav-1-2.jpg`
  - `/images/scraped/sailing/hav-2-2.jpg`
  - `/images/scraped/sailing/hav-4-2.jpg`
  - `/images/scraped/sailing/hav-7-2.jpg`
- Each image: use next/image with width={600} height={400}, rounded-2xl, object-cover, proper alt text describing the scene (e.g. "Gjester ombord i Havsul i Arendals skjaergaard", "Havsul seiler forbi Merdoe"). Use Norwegian characters in alt text.
- Apply the same styling patterns as the rest of the page: max-w-6xl mx-auto px-6, font-space headings, bg-cream or bg-warm-white background to alternate with surrounding sections.

**2. JSON-LD TouristTrip schema** - Add immediately after the opening `<main>` tag, using the same `dangerouslySetInnerHTML` pattern from the homepage:
```json
{
  "@context": "https://schema.org",
  "@type": "TouristTrip",
  "name": "Seiltur med Listerskoeyta Havsul",
  "description": "Kulturvandring i Arendals skjaergaard med den historiske Listerkoyta Havsul fra 1933. Historiefortelling om seilskutetiden, losvirksomhet og uthavner.",
  "touristType": "Cultural",
  "provider": {
    "@type": "TouristAttraction",
    "name": "Wannado",
    "url": "https://wannado.no",
    "telephone": "+4799469957",
    "address": {
      "@type": "PostalAddress",
      "streetAddress": "Skarestrand 11",
      "addressLocality": "Kongshavn",
      "postalCode": "4812",
      "addressCountry": "NO"
    }
  },
  "offers": [
    {
      "@type": "Offer",
      "name": "Seiltur per person",
      "price": "350",
      "priceCurrency": "NOK",
      "eligibleQuantity": {
        "@type": "QuantitativeValue",
        "maxValue": 12,
        "unitText": "personer"
      }
    },
    {
      "@type": "Offer",
      "name": "Charter Havsul",
      "price": "3500",
      "priceCurrency": "NOK",
      "unitText": "per time"
    }
  ],
  "itinerary": {
    "@type": "ItemList",
    "itemListElement": [
      {"@type": "ListItem", "position": 1, "name": "Avgang fra Pollen i Arendal"},
      {"@type": "ListItem", "position": 2, "name": "Seilas forbi Merdoe"},
      {"@type": "ListItem", "position": 3, "name": "Brattekleiv Skipsverft"}
    ]
  },
  "image": "https://wannado.no/images/havsul.jpg"
}
```
NOTE: Use proper Norwegian characters (oe -> o with slash, ae -> ae) in the actual JSON-LD. The description above uses ASCII for readability. Ensure the JSON uses real UTF-8 Norwegian: "Listerskoeyta" should be "Listerskoeyta Havsul" - wait, the correct Norwegian is "Listerskoeyta" -> actually use the exact term from the page: "Listerskoeyta Havsul". Check the existing page text for the exact spelling used there ("Listerskoeyta").

**3. CTA query parameter** - Update ALL Link components that point to `/kontakt`:
- Change `href="/kontakt"` to `href="/kontakt?tjeneste=Seilturer"` in both the pricing card CTA and the bottom CTA banner section.
- There are exactly 2 Link elements pointing to /kontakt: one in the pricing card ("Bestill seiltur") and one in the CTA banner ("Bestill naa").

Keep all existing content, styling, and structure intact. Do not remove or reorganize existing sections.
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Page has image gallery section with 6 real sailing photos using next/image
- JSON-LD TouristTrip schema present in page source with correct prices (350 NOK, 3500 NOK)
- Both CTA links point to /kontakt?tjeneste=Seilturer
- Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add defaultService prop to ContactForm for CTA pre-selection</name>
  <files>src/components/ui/ContactForm.tsx, src/app/kontakt/page.tsx</files>
  <action>
Modify ContactForm to accept and use a service pre-selection from URL query params:

**ContactForm.tsx changes:**
1. Add `useSearchParams` from `next/navigation` import.
2. Add an optional `defaultService` prop to the component: `interface ContactFormProps { defaultService?: string; }` and update the function signature.
3. Inside the component, read the URL search param: `const searchParams = useSearchParams();` and compute the initial service value: `const initialService = defaultService || searchParams.get("tjeneste") || "";`
4. The select element's `defaultValue` should use `initialService` instead of the hardcoded empty string `""`.
5. Validate that `initialService` is one of the `serviceOptions` values before using it (fall back to "" if not).
6. Wrap the form in a `Suspense` boundary to handle the `useSearchParams` requirement in Next.js 16. Actually, since ContactForm is already "use client" and useSearchParams requires Suspense at the page level, the cleaner approach is: wrap the `<ContactForm />` usage in kontakt/page.tsx with `<Suspense>`.

**kontakt/page.tsx changes:**
1. Import `Suspense` from `react`.
2. Wrap `<ContactForm />` in `<Suspense fallback={<div className="bg-cream rounded-2xl p-6 md:p-8 h-[500px] animate-pulse" />}>`.
3. No other changes needed; the form will read `?tjeneste=Seilturer` automatically from the URL.

This approach means NO changes needed to the seilturer page CTA links beyond what Task 1 already does. The ContactForm reads the query param itself.
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- ContactForm reads ?tjeneste= query parameter from URL
- Visiting /kontakt?tjeneste=Seilturer pre-selects "Seilturer" in the dropdown
- Visiting /kontakt without query param still shows "Velg tjeneste" placeholder
- Build succeeds with no errors or warnings about useSearchParams
  </done>
</task>

</tasks>

<verification>
After both tasks complete, verify the full phase success criteria:

1. `grep -c "application/ld+json" src/app/seilturer/page.tsx` returns 1 (JSON-LD present)
2. `grep -c "TouristTrip" src/app/seilturer/page.tsx` returns at least 1
3. `grep -c "tjeneste=Seilturer" src/app/seilturer/page.tsx` returns 2 (both CTAs)
4. `grep -c "scraped/sailing" src/app/seilturer/page.tsx` returns 6 (gallery images)
5. `grep "useSearchParams\|defaultService\|tjeneste" src/components/ui/ContactForm.tsx` shows query param reading
6. `npm run build` succeeds
</verification>

<success_criteria>
- Hero uses real Havsul image (already true, preserved)
- 6 real sailing photos displayed in responsive gallery grid
- Prices correct: kr 350,- per person, kr 3 500,- charter per time
- Practical info correct: Pollen i Arendal, 2-3 timer, maks 12 personer
- JSON-LD TouristTrip schema with both price offers in page source
- CTA buttons link to /kontakt?tjeneste=Seilturer
- ContactForm pre-selects "Seilturer" when accessed via that URL
- npm run build passes cleanly
</success_criteria>

<output>
After completion, create `.planning/phases/07-seilturer/07-01-SUMMARY.md`
</output>
