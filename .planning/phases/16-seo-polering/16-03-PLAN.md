---
phase: 16-seo-polering
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/app/layout.tsx
autonomous: true
requirements:
  - SEO-07

must_haves:
  truths:
    - "Vercel Analytics script is injected on every page via root layout"
    - "Analytics data appears in Vercel dashboard after deployment"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Analytics component rendered in body"
      contains: "Analytics"
    - path: "package.json"
      provides: "@vercel/analytics dependency"
      contains: "@vercel/analytics"
  key_links:
    - from: "src/app/layout.tsx"
      to: "@vercel/analytics"
      via: "import { Analytics } from '@vercel/analytics/next'"
      pattern: "Analytics"
---

<objective>
Integrate Vercel Analytics for privacy-friendly, cookieless website analytics.

Purpose: Track page views and visitor patterns without cookies or GDPR consent requirements. Essential for measuring SEO improvements and understanding user behavior.
Output: @vercel/analytics installed and rendered in root layout.
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/layout.tsx
@package.json

<interfaces>
From src/app/layout.tsx:
```typescript
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="nb" className={`${inter.variable} ${spaceGrotesk.variable} ${dmSerif.variable} scroll-smooth`}>
      <body className="antialiased text-brown bg-cream">
        {children}
      </body>
    </html>
  );
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install and configure Vercel Analytics</name>
  <files>
    package.json
    src/app/layout.tsx
  </files>
  <action>
**Step 1: Install the package.**
Run: `npm install @vercel/analytics`

**Step 2: Add Analytics component to root layout.**
In src/app/layout.tsx:

1. Add import at top: `import { Analytics } from '@vercel/analytics/next';`
2. Add `<Analytics />` inside `<body>`, after `{children}`:

```typescript
<body className="antialiased text-brown bg-cream">
  {children}
  <Analytics />
</body>
```

That is all. Vercel Analytics is zero-config: it automatically works when deployed to Vercel, and is a no-op in development. No API keys, no environment variables, no dashboard configuration needed.

Do NOT install @vercel/speed-insights (not requested, keep scope minimal).
Do NOT add Google Analytics (Vercel Analytics is cookieless and GDPR-friendly, which aligns with the personvern page stating no tracking cookies).
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - @vercel/analytics is in package.json dependencies
    - Analytics component is imported from '@vercel/analytics/next' in layout.tsx
    - Analytics component renders after {children} in body
    - Build passes without errors
    - No cookies, no API keys, no env vars needed
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `grep -r "Analytics" src/app/layout.tsx` shows import and component usage
3. `grep "@vercel/analytics" package.json` shows the dependency
</verification>

<success_criteria>
- @vercel/analytics installed as dependency
- Analytics component renders in root layout on every page
- Build passes clean
- No tracking cookies added (Vercel Analytics is cookieless by design)
</success_criteria>

<output>
After completion, create `.planning/phases/16-seo-polering/16-03-SUMMARY.md`
</output>
