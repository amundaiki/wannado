---
phase: 15-blogg-cms
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/sanity/schemaTypes/blogPost.ts
  - src/sanity/schemaTypes/service.ts
  - src/sanity/schemaTypes/testimonial.ts
  - src/sanity/schemaTypes/teamMember.ts
  - src/sanity/schemaTypes/index.ts
  - src/sanity/client.ts
  - src/sanity/image.ts
  - src/sanity/env.ts
  - src/sanity/lib/queries.ts
  - src/app/studio/[[...tool]]/page.tsx
  - src/app/studio/[[...tool]]/layout.tsx
  - .env.local
  - next.config.ts
  - package.json
autonomous: false
requirements:
  - CMS-01
  - CMS-02
  - CMS-03
  - CMS-04

user_setup:
  - service: sanity
    why: "Headless CMS for blog and content management"
    env_vars:
      - name: NEXT_PUBLIC_SANITY_PROJECT_ID
        source: "Output from npx sanity@latest init"
      - name: NEXT_PUBLIC_SANITY_DATASET
        source: "Output from npx sanity@latest init (usually 'production')"

must_haves:
  truths:
    - "Sanity project exists with project ID and dataset configured"
    - "Content schemas define blogPost, service, testimonial, teamMember"
    - "Sanity Studio is accessible at /studio in the browser"
    - "Sanity client can connect to the project and query content"
  artifacts:
    - path: "src/sanity/schemaTypes/blogPost.ts"
      provides: "Blog post content schema with title, slug, excerpt, body (Portable Text), mainImage, publishedAt, author"
      exports: ["blogPostType"]
    - path: "src/sanity/schemaTypes/service.ts"
      provides: "Service content schema with title, slug, description, image, price"
      exports: ["serviceType"]
    - path: "src/sanity/schemaTypes/testimonial.ts"
      provides: "Testimonial schema with quote, author, company, image"
      exports: ["testimonialType"]
    - path: "src/sanity/schemaTypes/teamMember.ts"
      provides: "Team member schema with name, role, bio, image, email, phone"
      exports: ["teamMemberType"]
    - path: "src/sanity/client.ts"
      provides: "Configured Sanity client for GROQ queries"
      exports: ["client"]
    - path: "src/sanity/image.ts"
      provides: "Sanity image URL builder"
      exports: ["urlFor"]
    - path: "src/app/studio/[[...tool]]/page.tsx"
      provides: "Sanity Studio route"
  key_links:
    - from: "src/app/studio/[[...tool]]/page.tsx"
      to: "src/sanity/schemaTypes/index.ts"
      via: "Sanity config importing schema types"
      pattern: "schemaTypes"
    - from: "src/sanity/client.ts"
      to: "src/sanity/env.ts"
      via: "Environment variables for project ID and dataset"
      pattern: "projectId.*dataset"
---

<objective>
Set up Sanity CMS with content schemas and embedded Studio for the Wannado website.

Purpose: Enable content management for blog posts, services, testimonials, and team members. This is the foundation for the blog (Plan 02) and future CMS-driven content across the site.
Output: Working Sanity Studio at /studio with four content schemas, configured Sanity client ready for queries.
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@next.config.ts
@package.json
@tsconfig.json
@src/app/layout.tsx
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User initializes Sanity project</name>
  <files>sanity.config.ts, sanity.cli.ts, package.json</files>
  <action>The user must run `npx sanity@latest init` interactively in the project root to create a Sanity project. This step requires browser-based authentication and project creation that cannot be automated.

Steps for the user:
1. Open terminal in /Users/amundrangoy/Prosjekter/wannado
2. Run: npx sanity@latest init
3. Follow prompts: create new project named "Wannado", dataset "production", embedded Studio
4. Note the project ID from the output
5. If it creates a sanity.config.ts and sanity.cli.ts in root, that is expected</action>
  <verify>ls sanity.config.ts sanity.cli.ts 2>/dev/null && grep -q "sanity" package.json && echo "Sanity initialized"</verify>
  <done>Sanity project exists, project ID is known, sanity packages are installed</done>
  <resume-signal>Provide the Sanity project ID and dataset name (e.g., "Project ID: abc123, dataset: production"), or type "done" if sanity init created config files automatically</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Configure Sanity client, schemas, and Studio route</name>
  <files>
    src/sanity/env.ts
    src/sanity/client.ts
    src/sanity/image.ts
    src/sanity/lib/queries.ts
    src/sanity/schemaTypes/blogPost.ts
    src/sanity/schemaTypes/service.ts
    src/sanity/schemaTypes/testimonial.ts
    src/sanity/schemaTypes/teamMember.ts
    src/sanity/schemaTypes/index.ts
    src/app/studio/[[...tool]]/page.tsx
    src/app/studio/[[...tool]]/layout.tsx
    .env.local
    next.config.ts
  </files>
  <action>
After the user completes `npx sanity@latest init`, inspect what files were created (likely sanity.config.ts, sanity.cli.ts, and possibly schema stubs). Build on top of what sanity init generated rather than conflicting with it.

**Environment config** (src/sanity/env.ts):
Create a module that exports `projectId` and `dataset` read from `process.env.NEXT_PUBLIC_SANITY_PROJECT_ID` and `process.env.NEXT_PUBLIC_SANITY_DATASET`. Include runtime validation that throws if either is missing. Also export `apiVersion` as `'2024-01-01'`.

**Create .env.local** with:
```
NEXT_PUBLIC_SANITY_PROJECT_ID=<from user>
NEXT_PUBLIC_SANITY_DATASET=production
```

**Sanity client** (src/sanity/client.ts):
Create a configured Sanity client using `next-sanity`'s `createClient`. Import projectId, dataset, apiVersion from env.ts. Set `useCdn: true` for production reads. Export `client`.

**Image URL builder** (src/sanity/image.ts):
Create image URL helper using `@sanity/image-url`. Import client. Export `urlFor` function that takes a Sanity image source and returns an ImageUrlBuilder. Usage pattern: `urlFor(source).width(800).url()`.

**Content schemas:**

blogPost.ts - defineType with:
- name: 'blogPost', title: 'Blogginnlegg'
- fields: title (string, required), slug (slug, source: title), excerpt (text, max 200 chars), mainImage (image with hotspot), body (array of block for Portable Text), publishedAt (datetime), author (string), categories (array of strings)
- preview: select title, media: mainImage

service.ts - defineType with:
- name: 'service', title: 'Tjeneste'
- fields: title (string), slug (slug), description (text), image (image with hotspot), price (string), features (array of strings)

testimonial.ts - defineType with:
- name: 'testimonial', title: 'Anbefaling'
- fields: quote (text, required), author (string), company (string), image (image)

teamMember.ts - defineType with:
- name: 'teamMember', title: 'Teammedlem'
- fields: name (string), role (string), bio (text), image (image with hotspot), email (string), phone (string), order (number for sorting)

index.ts - Export array `schemaTypes` containing all four imported types.

**GROQ queries** (src/sanity/lib/queries.ts):
Define and export reusable GROQ query strings:
- `POSTS_QUERY`: `*[_type == "blogPost"] | order(publishedAt desc)` with projection for title, slug, excerpt, mainImage, publishedAt, author
- `POST_BY_SLUG_QUERY`: `*[_type == "blogPost" && slug.current == $slug][0]` with full projection including body
- `POST_SLUGS_QUERY`: `*[_type == "blogPost" && defined(slug.current)][].slug.current`

**Studio route** (src/app/studio/[[...tool]]/page.tsx):
Use `next-sanity/studio` to render the Sanity Studio. Add `'use client'` directive. Import `NextStudio` from `next-sanity/studio` and the sanity config from the root `sanity.config.ts`. Export default component that renders `<NextStudio config={config} />`.

**Studio layout** (src/app/studio/[[...tool]]/layout.tsx):
Export metadata with `title: 'Wannado Studio'`. The layout should NOT include the site Navbar/Footer since Studio is a standalone app. Return just `{children}` without the root layout's body classes.

**Update next.config.ts:**
If `npx sanity@latest init` did not already handle this, ensure the config does NOT apply any security headers or content-security-policy that would block Sanity Studio's iframe/script loading. The existing `images.remotePatterns` for `cdn.sanity.io` is already present, which is good.

**Install packages** (if not already installed by sanity init):
Run `npm install next-sanity @sanity/image-url @sanity/vision` to ensure all required packages are present. The `sanity` core package should already be installed by the init step.

**Important:** All Norwegian text in schema titles and descriptions must use proper characters (title: 'Blogginnlegg', not 'Blogginnlegg').
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - src/sanity/ directory contains env.ts, client.ts, image.ts, lib/queries.ts
    - src/sanity/schemaTypes/ contains blogPost.ts, service.ts, testimonial.ts, teamMember.ts, index.ts
    - src/app/studio/[[...tool]]/ contains page.tsx and layout.tsx
    - .env.local contains NEXT_PUBLIC_SANITY_PROJECT_ID and NEXT_PUBLIC_SANITY_DATASET
    - `npm run build` succeeds without errors
    - /studio route compiles as part of the build
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Sanity Studio loads at /studio</name>
  <files>src/app/studio/[[...tool]]/page.tsx</files>
  <action>Verify that Sanity Studio loads correctly at /studio and all 4 content types are visible. Run `npm run dev` if not already running, then open http://localhost:3000/studio in a browser.</action>
  <verify>curl -s http://localhost:3000/studio | head -5</verify>
  <done>Sanity Studio loads at /studio with 4 content types visible: Blogginnlegg, Tjeneste, Anbefaling, Teammedlem. Main site still works at http://localhost:3000.</done>
  <what-built>Sanity CMS with 4 content schemas (blogPost, service, testimonial, teamMember) and embedded Sanity Studio at /studio</what-built>
  <how-to-verify>
    1. Run `npm run dev` in the project root
    2. Open http://localhost:3000/studio in a browser
    3. Verify Sanity Studio loads (may require Sanity login)
    4. Verify you see 4 content types in the left sidebar: Blogginnlegg, Tjeneste, Anbefaling, Teammedlem
    5. Try creating a test blog post with title, slug, excerpt, and some body text
    6. Verify the main site still works at http://localhost:3000
  </how-to-verify>
  <resume-signal>Type "approved" if Studio loads with all 4 content types, or describe any issues</resume-signal>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. /studio route is included in the build output
3. Sanity Studio loads at http://localhost:3000/studio
4. All 4 content types are visible and functional in the Studio
5. Sanity client can be imported and used (verified by build success)
</verification>

<success_criteria>
- Sanity project created and configured with environment variables
- 4 content schemas (blogPost, service, testimonial, teamMember) defined and registered
- Sanity Studio accessible at /studio with working content editing
- Sanity client module ready for queries from blog pages (Plan 02)
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-blogg-cms/15-01-SUMMARY.md`
</output>
