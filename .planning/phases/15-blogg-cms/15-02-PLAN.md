---
phase: 15-blogg-cms
plan: 02
type: execute
wave: 2
depends_on:
  - "15-01"
deferred_until_after:
  - "17-02"
files_modified:
  - src/app/blog/page.tsx
  - src/app/blog/[slug]/page.tsx
  - src/components/blog/BlogCard.tsx
  - src/components/blog/PortableTextRenderer.tsx
autonomous: true
requirements:
  - PAGE-08
  - PAGE-09

must_haves:
  truths:
    - "/blog shows a list of blog posts fetched from Sanity"
    - "/blog/[slug] renders a full article with Portable Text content"
    - "Blog pages use ISR with revalidate: 3600 for performance"
    - "Blog posts have proper SEO metadata (title, description, OG image)"
    - "Empty blog state shows a friendly message, not an error"
  artifacts:
    - path: "src/app/blog/page.tsx"
      provides: "Blog listing page with ISR"
      exports: ["default", "metadata"]
      min_lines: 40
    - path: "src/app/blog/[slug]/page.tsx"
      provides: "Individual blog post page with Portable Text rendering and ISR"
      exports: ["default", "generateMetadata", "generateStaticParams"]
      min_lines: 60
    - path: "src/components/blog/BlogCard.tsx"
      provides: "Reusable blog post card component for listing page"
      exports: ["default"]
    - path: "src/components/blog/PortableTextRenderer.tsx"
      provides: "Portable Text renderer with styled blocks"
      exports: ["default"]
  key_links:
    - from: "src/app/blog/page.tsx"
      to: "src/sanity/client.ts"
      via: "GROQ query for all posts"
      pattern: "client\\.fetch.*POSTS_QUERY"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/sanity/client.ts"
      via: "GROQ query for single post by slug"
      pattern: "client\\.fetch.*POST_BY_SLUG_QUERY"
    - from: "src/app/blog/[slug]/page.tsx"
      to: "src/components/blog/PortableTextRenderer.tsx"
      via: "Rendering Portable Text body content"
      pattern: "PortableTextRenderer|PortableText"
    - from: "src/components/blog/BlogCard.tsx"
      to: "src/sanity/image.ts"
      via: "Building image URLs for post thumbnails"
      pattern: "urlFor"
---

<objective>
Build the blog listing page (/blog) and individual article page (/blog/[slug]) that fetch content from Sanity CMS.

Purpose: Provide the SEO content marketing engine for Wannado. Blog posts about kayaking, sailing, teambuilding, and outdoor experiences in Arendal will drive organic traffic.
Output: Two working blog routes with ISR, Portable Text rendering, and SEO metadata.

Execution note: Deferred until after Phase 17 deploy and DNS cutover.
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/15-blogg-cms/15-01-SUMMARY.md

@src/app/layout.tsx
@src/app/seilturer/page.tsx
@src/components/layout/Navbar.tsx
@src/components/layout/Footer.tsx

<interfaces>
<!-- From Plan 01 outputs - Sanity client and queries -->

From src/sanity/client.ts:
```typescript
export const client: SanityClient;
```

From src/sanity/image.ts:
```typescript
export function urlFor(source: SanityImageSource): ImageUrlBuilder;
```

From src/sanity/lib/queries.ts:
```typescript
export const POSTS_QUERY: string;        // All posts, ordered by publishedAt desc
export const POST_BY_SLUG_QUERY: string; // Single post by slug parameter $slug
export const POST_SLUGS_QUERY: string;   // All slugs for generateStaticParams
```

From src/sanity/schemaTypes/blogPost.ts:
```typescript
// Blog post fields: title, slug, excerpt, mainImage, body (Portable Text), publishedAt, author, categories
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BlogCard component and PortableTextRenderer</name>
  <files>
    src/components/blog/BlogCard.tsx
    src/components/blog/PortableTextRenderer.tsx
  </files>
  <action>
**BlogCard component** (src/components/blog/BlogCard.tsx):
A server component (no 'use client') that displays a blog post preview card. Props: `post` object with `{ title, slug: { current: string }, excerpt, mainImage, publishedAt, author }`.

Layout:
- Wrapped in a Next.js Link to `/blog/${post.slug.current}`
- If mainImage exists, render it using next/image with `urlFor(post.mainImage).width(600).height(340).url()` as src. Use `fill` with `object-cover` in a relative container with aspect-ratio 16/9.
- Title as h2 with `font-space text-xl font-semibold text-brown`
- Excerpt as p with `text-brown/70 line-clamp-2`
- Date formatted as Norwegian locale string (e.g., "31. mars 2026") using `new Date(post.publishedAt).toLocaleDateString('nb-NO', { day: 'numeric', month: 'long', year: 'numeric' })`
- Author name if present
- Card styling: `bg-white rounded-2xl overflow-hidden shadow-sm hover:shadow-md transition-shadow`

Follow the design system: bg-cream background, text-brown for text, font-space for headings, font-inter for body text (these are CSS variables already set in layout.tsx).

**PortableTextRenderer** (src/components/blog/PortableTextRenderer.tsx):
A client component ('use client') that renders Sanity Portable Text. Use `@portabletext/react` package's `PortableText` component.

Install `@portabletext/react` if not already present.

Props: `value` (Portable Text array from Sanity body field).

Define custom components object for PortableText:
- block.h1: `<h1 className="font-space text-3xl font-bold text-brown mt-8 mb-4">`
- block.h2: `<h2 className="font-space text-2xl font-semibold text-brown mt-6 mb-3">`
- block.h3: `<h3 className="font-space text-xl font-semibold text-brown mt-5 mb-2">`
- block.normal: `<p className="text-brown/80 leading-relaxed mb-4">`
- block.blockquote: `<blockquote className="border-l-4 border-teal pl-4 italic text-brown/70 my-6">`
- list.bullet: `<ul className="list-disc pl-6 mb-4 space-y-1 text-brown/80">`
- list.number: `<ol className="list-decimal pl-6 mb-4 space-y-1 text-brown/80">`
- marks.link: `<a>` with `className="text-teal underline hover:text-teal/80"` and `href`, `target="_blank"`, `rel="noopener noreferrer"` for external links
- marks.strong: `<strong className="font-semibold">`
- marks.em: `<em>`
- types.image: Render using next/image with urlFor, wrapped in a `<figure>` with optional caption

Export default component that renders `<PortableText value={value} components={components} />`.
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx tsc --noEmit 2>&1 | tail -20</automated>
  </verify>
  <done>
    - BlogCard.tsx renders post preview with image, title, excerpt, date, link
    - PortableTextRenderer.tsx renders Portable Text with styled headings, paragraphs, lists, links, and images
    - Both files compile without TypeScript errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /blog listing page and /blog/[slug] article page with ISR</name>
  <files>
    src/app/blog/page.tsx
    src/app/blog/[slug]/page.tsx
  </files>
  <action>
**Blog listing page** (src/app/blog/page.tsx):
Server component with static metadata:
```
title: "Blogg - Opplevelser og inspirasjon | Wannado"
description: "Les om opplevelser i Arendals skjergaard, kajakkurs, seilturer med Havsul, teambuilding og friluftsliv paa Soerlandet."
```
(Use proper aeoeaa in the actual Norwegian text.)

Set ISR: `export const revalidate = 3600;`

Fetch posts: `const posts = await client.fetch(POSTS_QUERY)` from src/sanity/client.ts and src/sanity/lib/queries.ts.

Page structure:
- Import and render Navbar at top
- Hero section: simple header with `<h1>` "Blogg" and subtitle "Historier, tips og opplevelser fra Arendals skjaergaard" (proper aeoeaa). Style: `py-20 bg-cream text-center`, h1 with `font-dm text-5xl text-brown italic`, subtitle with `text-brown/70 mt-4 max-w-2xl mx-auto`.
- Posts grid: `<div className="max-w-6xl mx-auto px-4 py-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">` rendering BlogCard for each post.
- Empty state: If no posts, show a friendly message: "Ingen blogginnlegg ennaa. Kom tilbake snart!" with a link back to forsiden.
- Import and render Footer at bottom

**Blog article page** (src/app/blog/[slug]/page.tsx):
Server component with dynamic metadata via `generateMetadata`:
- Fetch post by slug using POST_BY_SLUG_QUERY with `{ slug: params.slug }`
- Return metadata: title from post title, description from post excerpt, openGraph.images from post mainImage using urlFor

Set ISR: `export const revalidate = 3600;`

Implement `generateStaticParams`:
- Fetch all slugs using POST_SLUGS_QUERY
- Return array of `{ slug }` objects

Page structure:
- Import and render Navbar
- Article wrapper: `<article className="max-w-3xl mx-auto px-4 py-12">`
- If mainImage exists, render hero image with next/image, `width={1200} height={630}` with `className="w-full rounded-2xl mb-8 object-cover"`
- Title: `<h1 className="font-dm text-4xl md:text-5xl text-brown italic mb-4">`
- Meta line: date (Norwegian format) and author, styled `text-brown/60 mb-8`
- Body: `<PortableTextRenderer value={post.body} />`
- Back link at bottom: "Tilbake til bloggen" linking to /blog, styled as teal link
- Import and render Footer

Handle 404: If post not found (query returns null), call `notFound()` from `next/navigation`.

**Important patterns from this codebase:**
- Pages use `<main className="min-h-screen bg-cream selection:bg-sand selection:text-brown">` as outer wrapper
- Navbar and Footer are imported from @/components/layout/
- Headings use font-dm (italic) or font-space (sans)
- Body text uses text-brown (the brown color #48382B)
- Accent color is teal (#00838f)
- Background is bg-cream (#faf8f4)
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npm run build 2>&1 | tail -30</automated>
  </verify>
  <done>
    - /blog route exists and builds successfully
    - /blog/[slug] route exists with generateStaticParams and generateMetadata
    - Both pages set revalidate: 3600 for ISR
    - Build output shows /blog and /blog/[slug] routes
    - Empty state handled gracefully when no posts exist
    - 404 handled when slug not found
  </done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with /blog and /blog/[slug] in route output
2. `npm run dev` shows /blog page with empty state or test posts
3. /blog/[slug] renders Portable Text content when a test post exists
4. ISR is configured (revalidate: 3600 in both page files)
5. SEO metadata is set on both pages
</verification>

<success_criteria>
- /blog lists all blog posts from Sanity with image, title, excerpt, date
- /blog/[slug] renders full article with Portable Text, hero image, metadata
- Both pages use ISR with 1-hour revalidation
- Both pages include proper SEO metadata (title, description, OG image)
- Empty and 404 states handled gracefully
- Build passes with zero errors
</success_criteria>

<output>
After completion, create `.planning/phases/15-blogg-cms/15-02-SUMMARY.md`
</output>
