---
phase: 08-kajakkturer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/kajakkturer/page.tsx
  - src/components/ui/ContactForm.tsx
autonomous: true
requirements:
  - PAGE-03
  - CONT-04
  - SEO-06

must_haves:
  truths:
    - "Hero-seksjonen viser ekte kajakkbilde fra scraped/kayak/, ikke placeholder"
    - "Hvert kurskort har et relevant bilde som matcher kursnivået"
    - "Guidede turer (Skjærgårdssafari, Fyr til fyr) har landskapsbilder fra skjærgården"
    - "JSON-LD Product schema finnes for hvert kurs med navn, pris og beskrivelse"
    - "CTA-knappen leder til /kontakt?tjeneste=Kajakktur og kontaktskjemaet forhåndsvelger riktig tjeneste"
    - "Roar Laugerds guideprofil bruker ekte foto i stedet for placeholder-ikon"
  artifacts:
    - path: "src/app/kajakkturer/page.tsx"
      provides: "Komplett kajakk-side med ekte bilder, JSON-LD Product schema, og CTA med forhåndsvalg"
      min_lines: 400
    - path: "src/components/ui/ContactForm.tsx"
      provides: "Kontaktskjema som leser searchParams for forhåndsvalgt tjeneste"
      contains: "searchParams"
  key_links:
    - from: "src/app/kajakkturer/page.tsx"
      to: "/kontakt?tjeneste=Kajakktur"
      via: "Link href med query parameter"
      pattern: "kontakt.*tjeneste=Kajakktur"
    - from: "src/components/ui/ContactForm.tsx"
      to: "URL searchParams"
      via: "useSearchParams hook reads tjeneste param"
      pattern: "useSearchParams|searchParams"
    - from: "src/app/kajakkturer/page.tsx"
      to: "schema.org Product"
      via: "JSON-LD script tag"
      pattern: "application/ld.json.*Product"
---

<objective>
Polish the kayak page (/kajakkturer) with real images from the scraped kayak photo library, add JSON-LD Product schema for each course, and wire the CTA button to pre-select "Kajakktur" on the contact form.

Purpose: Transform the existing kayak page from placeholder/single-image state to a visually rich, SEO-optimized page that converts visitors into booking inquiries. This is one of Wannado's core services with 56 available photos and 7 course levels.

Output: Updated kajakkturer/page.tsx with real images on hero, course cards, guided tour cards, and guide profile. Updated ContactForm.tsx to accept pre-selected service via URL search params. JSON-LD Product schema for all courses.
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@content/scraped/kajakkurs.md
@content/scraped/kajakkturer.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/components/ui/ContactForm.tsx (current, will be modified):
```typescript
// Current: no props, hardcoded serviceOptions
const serviceOptions = [
  "Seilturer", "Kajakktur", "Kajakkurs", "Teambuilding", "Foredrag/JTI", "Annet",
];
export default function ContactForm() { ... }
// Target: accept optional defaultService prop or read from searchParams
```

From src/app/kajakkturer/page.tsx (current structure):
```typescript
// Hero: uses /images/kajakk-kurs-bilde.jpg (rescue drill photo, not ideal for hero)
// Courses: array of 6 course objects, no images
// Guided tours: 2 cards with placeholder images
// Guide profile: SVG placeholder icon instead of real photo
// CTA: Link href="/kontakt" with no pre-selection
```

Existing JSON-LD pattern from src/app/page.tsx:
```typescript
<script
  type="application/ld+json"
  dangerouslySetInnerHTML={{
    __html: JSON.stringify({
      '@context': 'https://schema.org',
      '@type': 'TouristAttraction',
      // ...fields
    }),
  }}
/>
```

Available kayak images (verified by visual inspection):
```
HERO CANDIDATES (wide, scenic, energetic):
- kajakktur-strandhotell-4015.jpg  -> Group of kayakers in water waving, Norwegian coast town backdrop
- kajakk-bokhotellet-4533.jpg     -> Colorful kayaks beached in rocky cove, beautiful landscape

COURSE IMAGES (matched to course level):
- pusnes-introkurs-4613.jpg       -> Solo paddler in yellow kayak, calm water (Intro/Våttkort)
- kajakkurs-strandhotell-4010.jpg -> Kayaks lined up on beach ready for course (Grunnkurs hav)
- kajakkurs-redning-DSC_0165.jpg  -> Overhead shot of paddler in green water (Teknikkurs)
- bolgepadling-5570.jpg           -> Dramatic red kayak in heavy surf (Tøffe tak)
- kajakkursredning-3619.jpg       -> Rescue training in open water (Rullekurs)
- grebbestad-2013-3804.jpg        -> Campfire with kayaks, evening expedition mood (Turkurs/Ekspedisjon)

GUIDED TOUR IMAGES:
- kajakktur-16510307442.jpg       -> Kayakers by white Norwegian houses, calm fjord (Skjærgårdssafari)
- kajakktur-fyr-oslofjorden-3995.jpg -> Single paddler approaching lighthouse (Fyr til fyr)

GUIDE PROFILE:
- fyr-til-fyr-4656.jpg            -> Man (likely Roar) in kayak with cap and sunglasses

WARNING - NOT KAYAK PHOTOS (mislabeled in kayak/ folder, do NOT use):
- 53G0944.jpg, 53G0946.jpg, 53G0946-1.jpg, 53G0946-sep.jpg, 53G0949.jpg,
  53G0950.jpg, 53G0951.jpg, 53G0962.jpg, 53G0963.jpg -> These are horse riding photos
- 53G0070-scaled.jpg -> Metal anchor gate sculpture
- 53G0108-scaled.jpg -> Flags on beach
- 53G0425-scaled.jpg -> People building with rope on beach
- 53G1059.jpg, 53G1059-1.jpg, 53G1066.jpg, 53G1075.jpg -> Beach obstacles/sculpture
- P5140353.jpg -> Outdoor activity scene (not kayak)
- 15888718924.jpg -> Dinner scene indoors
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add images to hero, course cards, guided tours, and guide profile</name>
  <files>src/app/kajakkturer/page.tsx</files>
  <action>
Rewrite src/app/kajakkturer/page.tsx to integrate real images throughout the page. Keep all existing content, layout structure, and Tailwind styling patterns intact. Changes:

**1. Hero image:** Replace `/images/kajakk-kurs-bilde.jpg` (rescue drill) with `/images/scraped/kayak/kajakktur-strandhotell-4015.jpg` (group of kayakers waving at camera, energetic, wide format, Norwegian coastal town in background). Keep the same Image component props (fill, object-cover, priority, sizes="100vw").

**2. Course cards with images:** Add an `image` field to each course object in the `courses` array. Add a thumbnail Image to each course card above the text content:
- Våttkort/Introduksjonskurs: `/images/scraped/kayak/pusnes-introkurs-4613.jpg` (solo beginner paddler, calm water)
- Grunnkurs hav: `/images/scraped/kayak/kajakkurs-strandhotell-4010.jpg` (kayaks lined up on beach, course setup)
- Teknikkurs: `/images/scraped/kayak/kajakkurs-redning-DSC_0165.jpg` (overhead shot of paddler, technique focus)
- Rullekurs: `/images/scraped/kayak/kajakkursredning-3619.jpg` (rescue training in water)
- Tøffe tak: `/images/scraped/kayak/bolgepadling-5570.jpg` (dramatic red kayak in heavy surf)
- Turkurs/Ekspedisjon: `/images/scraped/kayak/grebbestad-2013-3804.jpg` (campfire with kayaks, expedition vibe)

For each course card, add a `div` with `relative h-40` wrapping an `Image` component with `fill`, `className="object-cover rounded-t-2xl"`, and `sizes="(max-width: 768px) 100vw, (max-width: 1024px) 50vw, 33vw"`. Adjust the card structure so the image sits above the padding area: wrap text content in a `div` with `p-8`, and the image div sits before it with no padding. Add `overflow-hidden` to the card's existing `rounded-2xl` container.

Each course image gets a descriptive Norwegian alt text, e.g., "Nybegynner padler kajakk i rolig vann ved Arendal", "Kajakkene klare for grunnkurs hav på stranden".

**3. Guided tour images:** Replace the two current placeholder-ish images:
- Skjærgårdssafari card: Replace `/images/kajakk-kurs-bilde.jpg` with `/images/scraped/kayak/kajakktur-16510307442.jpg` (kayakers paddling past white Norwegian houses in calm fjord). Alt: "Guidet kajakktur gjennom skjærgården med hvite sørlandshus"
- Fyr til fyr card: Replace `/images/gronland-kajakk-3.jpg` with `/images/scraped/kayak/kajakktur-fyr-oslofjorden-3995.jpg` (solo paddler approaching a lighthouse). Alt: "Kajakkpadler på vei mot fyrtårn langs kysten"

**4. Guide profile photo:** Replace the SVG placeholder circle with a real photo. Change the `div` with the SVG person icon to an `Image` component using `/images/scraped/kayak/fyr-til-fyr-4656.jpg` (man in kayak with cap and sunglasses). Use `width={96} height={96}` with `className="rounded-full object-cover"` and `alt="Roar Laugerud, kajakkguide og Veileder Hav"`. Keep the `flex-shrink-0` on the wrapper.

All images MUST use next/image `Image` component with explicit width/height or fill prop. No `img` tags.
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- Hero shows kajakktur-strandhotell-4015.jpg (group of kayakers, energetic)
- All 6 course cards have unique, level-appropriate images
- Skjærgårdssafari has fjord/house landscape image
- Fyr til fyr has lighthouse paddler image
- Roar's profile has real photo instead of SVG icon
- Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Add JSON-LD Product schema and wire CTA to pre-select contact form</name>
  <files>src/app/kajakkturer/page.tsx, src/components/ui/ContactForm.tsx</files>
  <action>
**Part A: JSON-LD Product schema in kajakkturer/page.tsx**

Add a `<script type="application/ld+json">` block at the top of the `<main>` element (same pattern as src/app/page.tsx). Create a JSON-LD array containing one Product per course, plus an ItemList wrapping them:

```
{
  "@context": "https://schema.org",
  "@type": "ItemList",
  "name": "Kajakkurs hos Wannado",
  "description": "Kajakkurs fra nybegynner til ekspedisjon i Arendals skjærgård",
  "numberOfItems": 6,
  "itemListElement": [
    {
      "@type": "Product",
      "position": 1,
      "name": "Våttkort / Introduksjonskurs",
      "description": "Perfekt start for nybegynnere. Lær grunnleggende padleteknikk, sikkerhet og selvberging. Du får våttkort etter bestått kurs.",
      "offers": {
        "@type": "Offer",
        "price": "1350",
        "priceCurrency": "NOK",
        "availability": "https://schema.org/InStock",
        "url": "https://wannado.no/kontakt?tjeneste=Kajakkurs"
      },
      "brand": {
        "@type": "Organization",
        "name": "Wannado"
      },
      "category": "Kajakkurs"
    },
    // ... repeat for each course with correct prices:
    // Grunnkurs hav: 3000 NOK
    // Teknikkurs: 3500 NOK
    // Rullekurs: 1250 NOK
    // Tøffe tak: 1350 NOK
    // Turkurs/Ekspedisjonskurs: 3500 NOK
  ]
}
```

Use `dangerouslySetInnerHTML={{ __html: JSON.stringify(schema) }}` pattern. Define the schema as a const above the return statement for readability. Ensure all prices are strings without commas or "Kr" prefix (just the number). Include `"url": "https://wannado.no/kontakt?tjeneste=Kajakkurs"` in each offer.

Also add a second JSON-LD block for the guided tours using TouristTrip schema:

```
{
  "@context": "https://schema.org",
  "@type": "TouristTrip",
  "name": "Guidede kajakkturer i Arendal",
  "description": "Guidede kajakkturer i skjærgården med erfaren veileder",
  "touristType": "Kayaking",
  "provider": {
    "@type": "Organization",
    "name": "Wannado",
    "telephone": "+4794826710"
  },
  "subTrip": [
    {
      "@type": "TouristTrip",
      "name": "Skjærgårdssafari",
      "description": "En guidet tur gjennom Arendals vakre skjærgård...",
      "offers": {
        "@type": "Offer",
        "price": "1350",
        "priceCurrency": "NOK"
      }
    },
    {
      "@type": "TouristTrip",
      "name": "Fyr til fyr",
      "description": "Padle mellom de historiske fyrene langs kysten..."
    }
  ]
}
```

**Part B: CTA link with pre-selection**

Change the CTA button Link in the bottom CTA section from:
```tsx
<Link href="/kontakt" ...>Bestill kurs eller tur</Link>
```
to:
```tsx
<Link href="/kontakt?tjeneste=Kajakktur" ...>Bestill kurs eller tur</Link>
```

**Part C: ContactForm reads searchParams**

Modify src/components/ui/ContactForm.tsx to accept an optional `defaultService` prop:

1. Add props interface: `interface ContactFormProps { defaultService?: string; }`
2. Change the function signature: `export default function ContactForm({ defaultService }: ContactFormProps)`
3. On the `<select>` element, change `defaultValue=""` to `defaultValue={defaultService || ""}`
4. The component remains "use client" as it already is

Then update src/app/kontakt/page.tsx to pass the searchParam through. BUT since page.tsx is a Server Component and ContactForm is a Client Component, create a thin wrapper:

Actually, simpler approach since ContactForm is already "use client": have ContactForm read the URL searchParams directly using `useSearchParams()` from `next/navigation`:

1. Import `useSearchParams` from `next/navigation`
2. Inside ContactForm, add: `const searchParams = useSearchParams();` and `const defaultService = searchParams.get('tjeneste') || '';`
3. Use `defaultService` as the `defaultValue` for the select element
4. Wrap the searchParams usage in a `Suspense` boundary if needed, or handle it gracefully

This way, navigating to `/kontakt?tjeneste=Kajakktur` will auto-select "Kajakktur" in the dropdown. No changes needed to kontakt/page.tsx.

Important: The `serviceOptions` array already contains "Kajakktur" and "Kajakkurs" as separate options. The CTA from the kajakk page should use "Kajakktur" for general booking, but course-specific CTAs (if any) could use "Kajakkurs". For this plan, the main CTA uses "Kajakktur".
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
- JSON-LD script tag exists with @type Product for each of the 6 courses
- Each Product has correct name, price (number string), priceCurrency NOK, and URL
- JSON-LD TouristTrip schema exists for guided tours
- CTA button links to /kontakt?tjeneste=Kajakktur
- ContactForm reads tjeneste from URL searchParams
- Visiting /kontakt?tjeneste=Kajakktur pre-selects "Kajakktur" in the dropdown
- Build succeeds with no errors
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes with no errors
2. All Image components reference files that exist in public/images/scraped/kayak/
3. JSON-LD is valid: parse the stringified JSON and verify @type Product with correct prices
4. grep for "kajakktur-strandhotell-4015" confirms hero image swap
5. grep for "tjeneste=Kajakktur" confirms CTA wiring
6. grep for "useSearchParams" in ContactForm confirms pre-selection support
</verification>

<success_criteria>
1. Hero uses real kayak image (kajakktur-strandhotell-4015.jpg), not placeholder
2. All 6 course cards display unique, contextually appropriate images
3. Both guided tour cards have landscape images from the kayak photo library
4. Roar's guide profile shows a real photo instead of SVG placeholder
5. JSON-LD Product schema exists for all 6 courses with correct NOK prices
6. JSON-LD TouristTrip schema exists for guided tours
7. CTA "Bestill kurs eller tur" links to /kontakt?tjeneste=Kajakktur
8. ContactForm auto-selects service when tjeneste query param is present
9. npm run build succeeds
</success_criteria>

<output>
After completion, create `.planning/phases/08-kajakkturer/08-01-SUMMARY.md`
</output>
