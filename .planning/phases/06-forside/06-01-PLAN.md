---
phase: 06-forside
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/HeroSection.tsx
  - src/components/sections/CarouselSection.tsx
  - src/components/sections/BentoGridSection.tsx
  - src/components/sections/FeatureSection.tsx
  - src/components/sections/PeopleSection.tsx
  - src/components/sections/ScrollVideoSection.tsx
autonomous: true
requirements:
  - PAGE-01
  - CONT-04

must_haves:
  truths:
    - "Hero-seksjonen viser et ekte Havsul/seilbilde, ikke en placeholder"
    - "Karuselkortene viser ekte bilder fra scraped/kayak/ og scraped/sailing/"
    - "BentoGrid-cellene viser ekte bilder fra scraped/ (teambuilding, kajakk, seil, landskap)"
    - "Alle bilder rendres via next/image med width, height og sizes-attributter"
    - "Hero-bildet har priority-prop for rask LCP"
    - "Forsiden scorer 90+ pa Lighthouse Performance"
  artifacts:
    - path: "src/components/sections/HeroSection.tsx"
      provides: "Hero med next/image og ekte bilde"
      contains: "import Image from"
    - path: "src/components/sections/CarouselSection.tsx"
      provides: "Karusell med next/image og scraped-bilder"
      contains: "import Image from"
    - path: "src/components/sections/BentoGridSection.tsx"
      provides: "BentoGrid med next/image og scraped-bilder"
      contains: "import Image from"
    - path: "src/components/sections/FeatureSection.tsx"
      provides: "FeatureSection med next/image"
      contains: "import Image from"
    - path: "src/components/sections/PeopleSection.tsx"
      provides: "PeopleSection med next/image"
      contains: "import Image from"
  key_links:
    - from: "src/components/sections/HeroSection.tsx"
      to: "public/images/scraped/sailing/baat-fb-havsul.jpg"
      via: "next/image src prop"
      pattern: "src=.*baat-fb-havsul"
    - from: "src/components/sections/CarouselSection.tsx"
      to: "public/images/scraped/kayak/"
      via: "next/image src prop in CAROUSEL_DATA"
      pattern: "scraped/kayak/"
    - from: "src/components/sections/BentoGridSection.tsx"
      to: "public/images/scraped/"
      via: "next/image src prop in BENTO_DATA"
      pattern: "scraped/"
---

<objective>
Replace all placeholder and raw `<img>` tags on the homepage with optimized `next/image` components using real scraped images from public/images/scraped/. The hero, carousel, and bento grid must show real photography from Wannado's operations (sailing, kayaking, teambuilding, landscape).

Purpose: This is the conversion-critical page. Real images build trust, next/image optimization ensures fast load times, and proper LCP handling keeps Lighthouse Performance above 90.
Output: Six updated section components, all using next/image with real scraped images.
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@src/app/page.tsx

<interfaces>
<!-- Current homepage section components all use raw <img> tags with eslint-disable.
     Each needs: import Image from "next/image", replace <img> with <Image>,
     add width/height/sizes props, remove eslint-disable comments. -->

From next.config.ts:
```typescript
images: {
  remotePatterns: [
    { protocol: 'https', hostname: 'cdn.sanity.io' }
  ],
}
// Local images in public/ are served directly, no remotePatterns needed.
```

Available scraped images (with dimensions):
```
SAILING:
  baat-fb-havsul.jpg       1200x630   (ideal hero: wide, Havsul boat)
  hav-4-2.jpg              1024x683   (good card: sailing scene)
  P9010367.jpg              1024x641   (good card: coastal)
  P7300114.jpg              1024x768   (good card: sailing)
  hav-1-2.jpg              1024x407   (wide banner, less useful for cards)

KAYAK (56 files, key ones):
  53G0425-scaled.jpg       2560x1707  (landscape, great quality)
  53G0108-scaled.jpg       1707x2560  (portrait)
  53G0070-scaled.jpg       1621x2560  (portrait)
  53G0860.jpg              1600x453   (very wide panorama)

TEAMBUILDING:
  Bungeerunning-7607.jpg   2250x1500  (action shot)
  DaVinchi-pa-Strand-8579.jpg  2250x1500  (group activity)
  Volum-liter-pa-Strand-Hotell-8538.jpg  2250x1500
  Personale-pa-Strand-Hotell-2830.jpg  (group)

LANDSCAPE:
  20230521_180051-2024-scaled.jpg  1920x2560  (portrait, scenic)
  DSC_0183-edited-scaled.jpg  (lighthouse area)
  fyr-188786.jpg  (lighthouse)

EXISTING IN ROOT (already used):
  baat-fb.jpg              1200x630
  havsul.jpg               1920x2560
  kajakk-kurs-bilde.jpg    1280x800
  tp-typeteori.jpg         1280x800
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace images in Hero, Carousel, and BentoGrid with next/image and real scraped photos</name>
  <files>
    src/components/sections/HeroSection.tsx
    src/components/sections/CarouselSection.tsx
    src/components/sections/BentoGridSection.tsx
  </files>
  <action>
    **HeroSection.tsx:**
    1. Add `import Image from "next/image"` at top.
    2. Replace the raw `<img src="/images/baat-fb.jpg">` with `<Image>` component:
       - Use `src="/images/scraped/sailing/baat-fb-havsul.jpg"` (1200x630, the Havsul boat, best hero candidate).
       - Set `fill` prop (since it fills the entire viewport with object-cover).
       - Set `priority={true}` (this is the LCP element).
       - Set `sizes="100vw"` (full-width hero).
       - Set `quality={85}`.
       - Keep `alt="Havsul under seil i Arendals skjaergaard"` (descriptive for SEO).
       - Keep the existing `className` for `object-cover`, but remove `w-full h-full` (not needed with fill).
       - The parent div already has `relative` and dimensions, so `fill` will work.
    3. Remove the `eslint-disable-next-line` comment.
    4. The component is "use client" with framer-motion, that's fine; next/image works in client components.

    **CarouselSection.tsx:**
    1. Add `import Image from "next/image"`.
    2. Update CAROUSEL_DATA image paths to use real scraped images:
       - Card 0 "Vinterkajakk & Sauna": `img: "/images/scraped/kayak/53G0425-scaled.jpg"` (2560x1707, landscape kayak shot)
       - Card 1 "Sunset & Stories": `img: "/images/scraped/sailing/hav-4-2.jpg"` (1024x683, sailing scene at sea)
       - Card 2 "Silent Sailing Retreat": `img: "/images/scraped/sailing/P9010367.jpg"` (1024x641, coastal sailing)
       - Card 3 "Skjaergaardseventyr": `img: "/images/scraped/sailing/P7300114.jpg"` (1024x768, sailing/coast)
    3. Add `width` and `height` fields to each CAROUSEL_DATA item matching the actual image dimensions.
    4. Replace the `<img>` inside the map with:
       ```tsx
       <Image
         src={item.img}
         alt={item.title}
         width={item.width}
         height={item.height}
         sizes="(max-width: 768px) 80vw, (max-width: 1024px) 50vw, 33vw"
         className="w-full h-full object-cover transition-transform duration-500 hover:scale-105"
       />
       ```
    5. Remove the `eslint-disable-next-line` comment.

    **BentoGridSection.tsx:**
    1. Add `import Image from "next/image"`.
    2. Update BENTO_DATA image paths and add width/height:
       - Item 0 "Skreddersydde arrangementer" (large, col-span-2): `img: "/images/scraped/teambuilding/DaVinchi-pa-Strand-8579.jpg"`, width: 2250, height: 1500
       - Item 1 "Ride & Reconnect": `img: "/images/scraped/landscape/20230521_180051-2024-scaled.jpg"`, width: 1920, height: 2560
       - Item 2 "Arendalsuka VIP": `img: "/images/scraped/sailing/baat-fb-havsul.jpg"`, width: 1200, height: 630
       - Item 3 "Kajakkurs for alle": `img: "/images/scraped/kayak/53G0425-scaled.jpg"`, width: 2560, height: 1707
       - Item 4 "Juleseilas": `img: "/images/scraped/sailing/hav-4-2.jpg"`, width: 1024, height: 683
    3. For the bento items, use `fill` prop instead of width/height since they are positioned with `absolute inset-0` inside sized containers. Each bento cell has a fixed height via grid/aspect-ratio. So the approach is:
       - Replace `<img>` with `<Image fill sizes="..." className="object-cover ..." />`
       - For the large (col-span-2) item: `sizes="(max-width: 768px) 100vw, 66vw"`
       - For single-column items: `sizes="(max-width: 768px) 100vw, 33vw"`
       - Keep `width` and `height` in data for reference, but use `fill` mode since images fill their containers.
    4. Remove all `eslint-disable-next-line` comments.
    5. Ensure each bento cell div wrapping the Image has `position: relative` (already the case since they use `relative` class).

    **IMPORTANT:** Do NOT change any layout, animations, text content, colors, or Tailwind classes beyond what's needed for the img-to-Image swap. Preserve all framer-motion animations, hover effects, and overlay gradients exactly as they are.
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -20</automated>
    Verify: build succeeds with zero errors. Grep the three files for: no remaining `<img` tags (only `<Image`), no `eslint-disable.*no-img-element`, all have `import Image from "next/image"`.
  </verify>
  <done>
    - HeroSection uses next/image with `/images/scraped/sailing/baat-fb-havsul.jpg`, fill mode, priority=true
    - CarouselSection uses next/image with 4 real scraped images from kayak/ and sailing/
    - BentoGridSection uses next/image with 5 real scraped images from teambuilding/, landscape/, sailing/, kayak/
    - Zero raw `<img>` tags remain in these three files
    - Build passes cleanly
  </done>
</task>

<task type="auto">
  <name>Task 2: Replace images in FeatureSection, PeopleSection, ScrollVideoSection and verify Lighthouse 90+</name>
  <files>
    src/components/sections/FeatureSection.tsx
    src/components/sections/PeopleSection.tsx
    src/components/sections/ScrollVideoSection.tsx
  </files>
  <action>
    **FeatureSection.tsx:**
    1. Add `import Image from "next/image"`.
    2. Replace the main `<img src="/images/tp-typeteori.jpg">` with:
       ```tsx
       <Image
         src="/images/scraped/teambuilding/Bungeerunning-7607.jpg"
         alt="Teambuilding med Wannado"
         width={2250}
         height={1500}
         sizes="(max-width: 768px) 100vw, 50vw"
         className="w-full aspect-[16/11] object-cover rounded-lg shadow-sm"
       />
       ```
    3. Replace the overlapping smaller `<img src="/images/20230910_125202-1280x960.jpg">` with:
       ```tsx
       <Image
         src="/images/scraped/teambuilding/Volum-liter-pa-Strand-Hotell-8538.jpg"
         alt="Kreativt samarbeid"
         width={2250}
         height={1500}
         sizes="(max-width: 768px) 60vw, 30vw"
         className="absolute -bottom-10 -right-6 md:-right-12 w-[60%] md:w-[55%] aspect-video object-cover rounded-lg border-4 border-card-bg shadow-xl"
       />
       ```
       Note: The overlapping image uses `absolute` positioning. Since the parent has `relative`, this will work. The `w-[60%]` and aspect-video classes control the rendered size.
    4. Remove both `eslint-disable-next-line` comments.

    **PeopleSection.tsx:**
    1. Add `import Image from "next/image"`.
    2. Add `width` and `height` to each PEOPLE data item. Keep the existing image paths (`/images/tp-typeteori.jpg` for Tone, `/images/P7270038.jpg` for Roar) since these are actual photos of the named individuals, not placeholders.
       - Tone: width: 1280, height: 800
       - Roar: width: 1280, height: 960 (check actual dimensions of P7270038.jpg)
    3. Replace `<img>` with:
       ```tsx
       <Image
         src={person.img}
         alt={person.name}
         width={person.width}
         height={person.height}
         sizes="(max-width: 768px) 100vw, 50vw"
         className="w-full aspect-[4/5] object-cover rounded-xl shadow-lg hover:shadow-xl transition-shadow duration-500"
       />
       ```
    4. Remove `eslint-disable-next-line` comment.

    **ScrollVideoSection.tsx:**
    1. The poster fallback `<img src="/images/havsul-poster.jpg">` needs next/image treatment.
    2. Add `import Image from "next/image"`.
    3. Replace the poster `<img>` with:
       ```tsx
       <Image
         src="/images/havsul-poster.jpg"
         alt="Listerskoyten Havsul"
         fill
         sizes="100vw"
         className="object-cover"
       />
       ```
       The parent div already has appropriate sizing. Keep `absolute inset-0` classes or use fill mode.
    4. Remove `eslint-disable-next-line` comment.
    5. Note: The canvas-based scroll video frames load via `new Image()` in JS; these are NOT rendered via `<img>` tags and should NOT be converted to next/image. Leave the canvas/frame loading logic untouched.

    **Performance verification:**
    After all image replacements, run `npx next build` and check for:
    - Zero build errors
    - Zero `@next/next/no-img-element` ESLint warnings from homepage sections
    - Confirm all images are being processed by Next.js image optimization

    Check actual image dimensions of P7270038.jpg before setting width/height:
    ```bash
    sips -g pixelWidth -g pixelHeight public/images/P7270038.jpg
    ```

    Verify no raw `<img>` tags remain in any homepage section component:
    ```bash
    grep -r '<img' src/components/sections/
    ```
    The only acceptable remaining `<img>` would be inside ScrollVideoSection's canvas logic (the `new Image()` in JS), which is not an HTML img tag.
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -30 && echo "--- IMG TAG CHECK ---" && grep -rn '<img' src/components/sections/ || echo "No raw img tags found in sections"</automated>
    Build must succeed. The grep should return zero matches (no raw `<img>` in section components). The `new Image()` in ScrollVideoSection JS is fine; grep looks for `<img` in JSX only.
  </verify>
  <done>
    - FeatureSection uses next/image with real teambuilding scraped images
    - PeopleSection uses next/image with correct width/height for portrait photos
    - ScrollVideoSection poster uses next/image with fill mode
    - Zero raw `<img>` tags remain in any homepage section component
    - Build passes with zero errors and zero no-img-element warnings
    - All homepage images served via Next.js image optimization pipeline (automatic WebP/AVIF, responsive srcset)
  </done>
</task>

</tasks>

<verification>
1. `npx next build` completes with zero errors
2. `grep -rn '<img' src/components/sections/` returns zero matches
3. `grep -rn 'eslint-disable.*no-img-element' src/components/sections/` returns zero matches
4. `grep -rn 'import Image from' src/components/sections/` returns matches for all 6 section files
5. `grep -rn 'scraped/' src/components/sections/` confirms real scraped images are referenced
6. `grep -rn 'priority' src/components/sections/HeroSection.tsx` confirms hero has priority prop
7. Run `npx next dev` and visit http://localhost:3000, inspect network tab: images should be served as WebP/AVIF with appropriate sizes
</verification>

<success_criteria>
1. Hero section displays the real Havsul sailing photo (baat-fb-havsul.jpg from scraped/sailing/)
2. Carousel section shows 4 real photos from scraped/kayak/ and scraped/sailing/
3. BentoGrid section shows 5 real photos from scraped/teambuilding/, scraped/landscape/, scraped/sailing/, scraped/kayak/
4. All images across all 6 homepage sections use next/image with width/height or fill
5. Hero image has priority={true} for fast LCP
6. Build passes cleanly with zero ESLint img warnings
7. Lighthouse Performance target: 90+ (next/image handles responsive srcset, WebP/AVIF, lazy loading automatically)
</success_criteria>

<output>
After completion, create `.planning/phases/06-forside/06-01-SUMMARY.md`
</output>
