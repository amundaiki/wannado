---
phase: 17-deploy
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - vercel.json
autonomous: true
requirements:
  - STRUCT-03
must_haves:
  truths:
    - "Vercel project exists and is linked to this repository"
    - "Preview deployment is live and accessible via Vercel URL"
    - "All 23 routes render correctly on preview deploy"
    - "301 redirects from old WordPress URLs work in production"
  artifacts:
    - path: ".vercel/project.json"
      provides: "Vercel project linking configuration"
      contains: "projectId"
    - path: "vercel.json"
      provides: "Vercel deployment configuration with headers and redirect verification"
  key_links:
    - from: "next.config.ts redirects()"
      to: "Vercel deployment"
      via: "Next.js build output"
      pattern: "301 redirect"
    - from: "npm run build"
      to: "Vercel preview URL"
      via: "vercel deploy"
      pattern: "https://.*vercel.app"
---

<objective>
Create Vercel project, deploy a preview build, and verify that the Next.js 16 app works correctly on Vercel infrastructure before DNS cutover.

Purpose: Establish the Vercel deployment pipeline and confirm everything works before touching DNS. This de-risks the production cutover by catching build or config issues early.
Output: Live preview deployment on *.vercel.app with all routes and redirects verified.
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@next.config.ts
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Vercel project and deploy preview</name>
  <files>vercel.json, .vercel/project.json</files>
  <action>
1. Link the project to Vercel using the CLI. The user is logged in as `amundaiki` on Vercel CLI v48.2.9:
   ```
   vercel link --yes
   ```
   This creates `.vercel/project.json`. When prompted for project name, use "wannado". Framework detection should auto-detect Next.js.

2. Create a minimal `vercel.json` in the project root with security headers and configuration:
   ```json
   {
     "$schema": "https://openapi.vercel.sh/vercel.json",
     "framework": "nextjs",
     "headers": [
       {
         "source": "/(.*)",
         "headers": [
           { "key": "X-Content-Type-Options", "value": "nosniff" },
           { "key": "X-Frame-Options", "value": "DENY" },
           { "key": "Referrer-Policy", "value": "strict-origin-when-cross-origin" }
         ]
       },
       {
         "source": "/images/(.*)",
         "headers": [
           { "key": "Cache-Control", "value": "public, max-age=31536000, immutable" }
         ]
       }
     ]
   }
   ```

3. Deploy a preview build:
   ```
   vercel deploy --yes
   ```
   Record the preview URL from the output (format: https://wannado-*.vercel.app).

4. No environment variables are needed. The current codebase has zero `process.env` references. The `.env` file contains only `APIFY_API_KEY` which was used for scraping in Phase 2 and is not needed in production.
  </action>
  <verify>
    <automated>vercel ls --yes 2>&1 | head -20</automated>
  </verify>
  <done>Vercel project "wannado" exists, preview deployment is live with a *.vercel.app URL, .vercel/project.json created locally</done>
</task>

<task type="auto">
  <name>Task 2: Verify preview deployment routes and redirects</name>
  <files></files>
  <action>
1. Get the preview URL from the previous deployment (from `vercel ls` output or deployment output).

2. Test that all key routes return 200 OK using curl against the preview URL:
   - `/` (homepage)
   - `/seilturer`
   - `/kajakkturer`
   - `/teambuilding`
   - `/foredrag`
   - `/kontakt`
   - `/opplevelser`
   - `/sitemap.xml`
   - `/robots.txt`

   Use: `curl -s -o /dev/null -w "%{http_code}" https://PREVIEW_URL/seilturer`

3. Test that 301 redirects from next.config.ts work:
   - `/kulturvandring-med-sjarmerende-treskoyte` should 301 to `/seilturer`
   - `/kajakk-turer-og-ekspedisjoner` should 301 to `/kajakkturer`
   - `/kurs/kajakkurs` should 301 to `/kajakkturer`

   Use: `curl -s -o /dev/null -w "%{http_code} %{redirect_url}" https://PREVIEW_URL/kulturvandring-med-sjarmerende-treskoyte`

4. Test that the API route responds:
   - `curl -s -X POST https://PREVIEW_URL/api/contact -H "Content-Type: application/json" -d '{"name":"test","email":"test@test.com","message":"test"}' -w "\n%{http_code}"`

5. If any route fails, investigate and fix before proceeding to Plan 02 (DNS cutover).
  </action>
  <verify>
    <automated>PREVIEW_URL=$(vercel ls --yes 2>&1 | grep -o 'https://[^ ]*\.vercel\.app' | head -1) && curl -s -o /dev/null -w "%{http_code}" "$PREVIEW_URL/seilturer"</automated>
  </verify>
  <done>All key routes return 200, all 3 redirects return 301 to correct destinations, API route responds</done>
</task>

</tasks>

<verification>
1. `vercel ls` shows the wannado project with at least one deployment
2. Preview URL loads the homepage with correct content
3. All 301 redirects from old WordPress URLs work
4. No build errors or warnings in Vercel deployment log
</verification>

<success_criteria>
- Vercel project linked and preview deployed
- All 23 routes accessible on preview URL
- 301 redirects verified working
- No environment variable configuration needed (zero process.env usage)
- Ready for DNS cutover in Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/17-deploy/17-01-SUMMARY.md`
</output>
