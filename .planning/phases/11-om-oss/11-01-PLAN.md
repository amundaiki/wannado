---
phase: 11-om-oss
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/om-oss/page.tsx
  - src/app/menneskene/page.tsx
  - src/components/sections/PeopleSection.tsx
  - next.config.ts
autonomous: true
requirements: [PAGE-10]

must_haves:
  truths:
    - "/om-oss viser Wannados historie, verdier og filosofi"
    - "/om-oss viser fulle profiler for Tone Pernille og Roar med bilder"
    - "/om-oss lister alle sertifiseringer: JTI, NLP Master Coach, Veileder Hav, Skipper, BCU"
    - "/menneskene redirecter til /om-oss med 301"
    - "Siden bruker next/image for alle bilder, ikke <img>"
    - "Metadata er SEO-optimalisert med riktig title og description"
  artifacts:
    - path: "src/app/om-oss/page.tsx"
      provides: "Combined Om oss page with team profiles, company history, certifications"
      min_lines: 120
    - path: "next.config.ts"
      provides: "301 redirect from /menneskene to /om-oss"
      contains: "menneskene"
  key_links:
    - from: "src/app/om-oss/page.tsx"
      to: "next/image"
      via: "Image component import"
      pattern: "import Image from .next/image."
    - from: "src/app/om-oss/page.tsx"
      to: "/kontakt"
      via: "CTA link"
      pattern: "href=./kontakt."
    - from: "next.config.ts"
      to: "/om-oss"
      via: "redirect config"
      pattern: "source.*menneskene.*destination.*/om-oss"
---

<objective>
Redesign /om-oss into a polished, combined page that replaces both the current /om-oss and /menneskene pages. The new page presents Wannado's history, values, and philosophy alongside full team profiles for Tone Pernille Sivertsen and Roar Laugerud, including their certifications and expertise areas. Add a 301 redirect from /menneskene to /om-oss.

Purpose: The current /om-oss page is a raw content dump from the PeopleSection component only. /menneskene is a separate raw dump of the same content. Combining them into one well-designed page improves SEO (no duplicate content), user experience, and brand storytelling.

Output: Redesigned /om-oss page, 301 redirect, cleaned up /menneskene route
</objective>

<execution_context>
@/Users/amundrangoy/.claude/get-shit-done/workflows/execute-plan.md
@/Users/amundrangoy/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md

@src/app/seilturer/page.tsx (reference for page structure, metadata pattern, hero section, CTA pattern)
@src/app/layout.tsx (fonts: font-space, font-inter, font-dm; colors: bg-cream, text-brown, text-teal, bg-sand)
@content/scraped/menneskene.md (source content for profiles)

<interfaces>
<!-- Existing patterns the executor must follow -->

From src/app/seilturer/page.tsx (page pattern):
```typescript
import type { Metadata } from "next";
import Image from "next/image";
import Link from "next/link";
import Navbar from "@/components/layout/Navbar";
import Footer from "@/components/layout/Footer";

export const metadata: Metadata = {
  title: "...",
  description: "...",
  openGraph: { title: "...", description: "...", images: ["..."] },
};
```

From src/app/layout.tsx (design tokens):
- Fonts: font-space (headings), font-inter (body), font-dm (italic accents)
- Colors: bg-cream (page bg), text-brown (headings), text-brown-mid (body), text-teal (accents), bg-sand (CTA bg), bg-warm-white (alt section bg)
- Patterns: rounded-2xl cards, border-border, shadow-lg for cards
- Layout: max-w-5xl or max-w-6xl mx-auto px-6

From next.config.ts (redirect pattern):
```typescript
async redirects() {
  return [
    { source: '/kulturvandring-med-sjarmerende-treskoyte', destination: '/seilturer', permanent: true },
  ];
}
```

Available profile images:
- `/images/tp-typeteori.jpg` (39KB, Tone Pernille) 
- `/images/P7270038.jpg` (279KB, Roar)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add /menneskene redirect and remove old page</name>
  <files>next.config.ts, src/app/menneskene/page.tsx</files>
  <action>
1. In `next.config.ts`, add a new redirect entry to the existing `redirects()` array:
   ```
   { source: '/menneskene', destination: '/om-oss', permanent: true }
   ```
   Keep all existing redirects intact. Do NOT change the `images` config.

2. Delete `src/app/menneskene/page.tsx` entirely. The /menneskene route will now be handled by the redirect in next.config.ts.

3. The `PeopleSection.tsx` component will no longer be used after Task 2 rebuilds /om-oss as a self-contained page. Delete `src/components/sections/PeopleSection.tsx` as well; it uses `<img>` tags (not next/image) and framer-motion unnecessarily.
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /menneskene redirect exists in next.config.ts with permanent: true
    - src/app/menneskene/page.tsx deleted
    - src/components/sections/PeopleSection.tsx deleted
    - Build succeeds with no errors
  </done>
</task>

<task type="auto">
  <name>Task 2: Rebuild /om-oss as combined team + company page</name>
  <files>src/app/om-oss/page.tsx</files>
  <action>
Rewrite `src/app/om-oss/page.tsx` as a self-contained page (no separate component file needed). Follow the exact patterns from seilturer/page.tsx for structure, imports, and styling. The page must be a Server Component (no "use client"). Use `next/image` for all images.

**Metadata:**
```typescript
export const metadata: Metadata = {
  title: "Om oss - Menneskene bak Wannado | Opplevelser i Arendal",
  description: "Bli kjent med Tone Pernille Sivertsen og Roar Laugerud. Over 15 års erfaring med kajakk, seil, teambuilding og hesteopplevelser i Arendal og langs Sørlandskysten.",
  openGraph: {
    title: "Om oss - Menneskene bak Wannado",
    description: "Bli kjent med Tone Pernille og Roar. Over 15 års erfaring med opplevelser i Arendals skjærgård.",
    images: ["/images/P7270038.jpg"],
  },
};
```

**Page sections in order:**

1. **Hero Section** - Brown background (bg-brown), NOT image hero (no good group photo available). Use the same pattern as the current om-oss page hero but with better copy:
   - Subtitle (small caps, font-inter, text-sand, tracking-[3px]): "OM WANNADO"
   - H1 (font-space, text-4xl md:text-6xl, font-bold, text-white): "Menneskene bak opplevelsene"
   - Intro paragraph (font-inter, text-lg, text-white/70, max-w-2xl mx-auto): "Over 15 år med lidenskap for mestring, lek og naturkraft. Fra Kongshavn i Arendal skaper vi opplevelser som setter spor."
   - Padding: pt-40 pb-20

2. **Company Story Section** (bg-cream, py-20 md:py-28):
   - max-w-4xl mx-auto px-6
   - H2: "Wannados historie"
   - Two-three paragraphs about Wannado's story using content from scraped menneskene.md combined with CLAUDE.md info:
     - Founded around 2005, based at Skarestrand in Kongshavn, Arendal
     - 15+ years of group activities; kayak tours, sailing with Havsul, teambuilding, horse experiences, lectures
     - Philosophy section using the scraped "Hva tenker vi?" content: "Lek er helsebringende, utviklende og mestringsgivende. Hva er DIN lek?"
   - Use font-dm italic for the "Hva er DIN lek?" quote as a styled pullquote (text-2xl md:text-3xl font-dm text-brown/80 text-center py-8 border-y border-border)

3. **Team Profiles Section** (bg-warm-white, py-20 md:py-28):
   - max-w-5xl mx-auto px-6
   - H2 centered: "Folkene bak opplevelsene"
   - Two profile cards in a grid (grid-cols-1 lg:grid-cols-2, gap-12 lg:gap-16)
   - Each profile card structure:
     - Image: `next/image`, rounded-xl, aspect-[4/5] via a wrapper div with relative + overflow-hidden. Use `width={600} height={750}` with `className="object-cover w-full h-full"`. NOT fill layout (avoids needing a sized container).
     - Name: font-space text-2xl md:text-3xl font-semibold text-brown, mt-6
     - Role subtitle: font-inter text-sm font-medium text-teal uppercase tracking-wider, mt-1
     - Bio: font-inter text-base md:text-lg text-brown-mid leading-relaxed, mt-4. Use the FULL scraped bio text from menneskene.md (not the shortened version from PeopleSection.tsx)
     - Certifications: A list of certification badges below the bio, mt-4. Use small inline elements: `<span className="inline-block bg-teal/10 text-teal font-inter text-xs font-semibold px-3 py-1.5 rounded-full mr-2 mb-2">JTI-sertifisert</span>`
     - Email: font-inter text-sm text-brown-muted hover:text-brown, mt-3

   **Tone Pernille Sivertsen profile:**
   - Image: `/images/tp-typeteori.jpg`, alt="Tone Pernille Sivertsen"
   - Role: "Teambuilding, hesteopplevelser og coaching"
   - Bio: Full text from scraped menneskene.md (both paragraphs about JTI/NLP coaching AND the horse/islandshester paragraph). Replace em dashes with comma or semicolon per CLAUDE.md rules.
   - Certifications: "JTI-sertifisert", "NLP Master Coach", "Dyreassisterte intervensjoner"
   - Email: tp@wannado.no
   - Phone: +47 99 46 99 57

   **Roar Laugerud profile:**
   - Image: `/images/P7270038.jpg`, alt="Roar Laugerud"
   - Role: "Kajakkinstruktor, skipper og fotograf"
   - Bio: Full text from scraped menneskene.md (both paragraphs about his expedition background AND his Veileder Hav/BCU/Skipper certifications). Replace em dashes with comma or semicolon.
   - Certifications: "Veileder Hav", "BCU nest hoyeste niva" (use ø: "BCU nest høyeste nivå"), "Fritidsskipper", "Autorisert for 12 passasjerer"
   - Email: roar@wannado.no
   - Phone: +47 94 82 67 10

4. **Values Section** (bg-cream, py-20 md:py-28):
   - max-w-5xl mx-auto px-6
   - H2 centered: "Våre verdier"
   - 3-column grid (grid-cols-1 sm:grid-cols-3, gap-8) with value cards (same card style as seilturer "Hva kan du forvente?" section: bg-white rounded-2xl p-8 border border-border):
     - **Trygghet**: Icon (shield-check SVG). "Sikkerhet først. Våre instruktorer har de høyeste sertifiseringene, og vi har over 15 års erfaring med trygge opplevelser på sjøen."
     - **Mestring**: Icon (trophy/star SVG). "Vi tror på at alle kan mer enn de tror. Våre opplevelser er designet for at du skal strekke deg og vokse."
     - **Lek**: Icon (sparkles/heart SVG). "Lek er helsebringende, utviklende og mestringsgivende. Vi skaper rom for den leken alle fortjener."

5. **CTA Section** (bg-brown, py-20 md:py-24):
   - Same pattern as seilturer CTA section
   - H2: "Vil du oppleve noe nytt?"
   - P: "Ta kontakt for en uforpliktende prat om hva vi kan skape sammen. Vi skreddersyr opplevelser for grupper, bedrifter og privatpersoner."
   - Two CTAs: "Ta kontakt" (Link to /kontakt, bg-sand text-brown) and "Ring +47 99 46 99 57" (phone link, border-2 border-white/30 text-white)

6. **Footer** - Standard `<Footer />` component

All text MUST use correct Norwegian characters (æøå). No em dashes anywhere. Mobile-first approach (base styles for mobile, md: and lg: breakpoints for larger screens).
  </action>
  <verify>
    <automated>cd /Users/amundrangoy/Prosjekter/wannado && npx next build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - /om-oss renders with hero, company story, team profiles, values, and CTA sections
    - Both Tone Pernille and Roar have full profiles with images using next/image
    - Certifications listed as styled badges: JTI, NLP Master Coach, Veileder Hav, Skipper, BCU
    - Wannado history and philosophy section with "Hva er DIN lek?" pullquote
    - Metadata has proper title and description for SEO
    - All Norwegian text uses æøå correctly
    - No em dashes in any text
    - Build succeeds
  </done>
</task>

</tasks>

<verification>
```bash
# 1. Build succeeds
cd /Users/amundrangoy/Prosjekter/wannado && npx next build

# 2. Redirect exists in config
grep -A2 "menneskene" next.config.ts

# 3. Page uses next/image (not <img>)
grep -c "import Image" src/app/om-oss/page.tsx

# 4. No PeopleSection import remains
grep -r "PeopleSection" src/ && echo "FAIL: PeopleSection still imported" || echo "OK: No PeopleSection imports"

# 5. Old menneskene page removed
test -f src/app/menneskene/page.tsx && echo "FAIL: menneskene still exists" || echo "OK: menneskene removed"

# 6. Metadata exists
grep "export const metadata" src/app/om-oss/page.tsx

# 7. No em dashes
grep -c "—" src/app/om-oss/page.tsx && echo "FAIL: em dash found" || echo "OK: no em dashes"

# 8. Certifications present
grep -c "JTI\|NLP\|Veileder Hav\|Skipper\|BCU" src/app/om-oss/page.tsx
```
</verification>

<success_criteria>
1. /om-oss shows a polished page with company history, team profiles, values, and CTA
2. Tone Pernille and Roar both have full-length bios from the scraped content, with profile images rendered via next/image
3. All certifications (JTI, NLP Master Coach, Veileder Hav, Skipper/12 passasjerer, BCU) are listed as badges
4. /menneskene redirects to /om-oss via 301 in next.config.ts
5. PeopleSection.tsx removed (functionality now inline in om-oss page)
6. `npm run build` passes without errors
7. All Norwegian text uses proper æøå characters
</success_criteria>

<output>
After completion, create `.planning/phases/11-om-oss/11-01-SUMMARY.md`
</output>
